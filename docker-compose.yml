services:
  discord-drive-uploader:
    build: .
    image: discord-drive-uploader:local
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      CLIENT_MAX_BODY_SIZE: 20M
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: always
